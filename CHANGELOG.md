# 更新日志

详细开发修复记录见 [docs/fix-log.md](docs/fix-log.md)，此处记录对外发布版本的变化。

## [1.1.4] - 2026-08-11

### 新增

- 输出上限可调：设置页「上下文」新增「输出上限」（512~384000，默认 8192），思考型模型长输出不再被打断。
- 内置预设新增 OpenCode Go（deepseek-v4-flash），建议输出上限 32768。
- 发送前按上下文预算裁剪历史：长会话继续对话不会因超出 `contextLimit` 被网关拒绝。

### 优化

- 缓存命中优化：当前时间移到 system prompt 末尾，稳定前缀跨分钟复用服务端缓存，命中率与首字速度提升。
- Markdown 解析缓存 + 流式刷新节流：长文输出更顺滑，动画与渲染体验不降级。
- 页面重建收敛：主页只监听初始化状态，聊天列表只监听消息版本，状态条 / token / 工具事件变化不再整页重建。

### 修复

- `fr=length` 且正文为空时改为整轮重试，并注入「直接输出结果」；首包超时从 30s 放宽到 60s。
- 网关拒绝过大的 `max_tokens` 时自动降级 8192 重试。

### 验证

- `flutter analyze` 无告警；`flutter test` 29 项全部通过。
- release 真机覆盖安装（`adb install -r`），数据保留；35 秒采样 218 帧、0 janky。

## 历史版本

- v1.1.0 / v1.1.1 及更早版本的变更与修复见 [docs/fix-log.md](docs/fix-log.md)。
